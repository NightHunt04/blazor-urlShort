@inject SharedStateService SharedState
@rendermode InteractiveServer

<div class="w-full md:w-[20%] h-[8%] md:h-full flex md:flex-col items-center justify-start gap-2 md:gap-4 px-3 py-2 md:px-5 md:py-4 rounded-2xl font-medium bg-[#fff] shadow-xl">
    <button @onclick="() => SetActiveComponent(1)" class="w-full text-center p-2 md:px-5 md:py-3 rounded-lg border-[1px] border-orange-600 transition-all duration-300 shadow-lg @(SharedState.ActiveComponent == 1 ? "bg-orange-600 text-white hover:bg-white hover:text-black" : "bg-white hover:bg-orange-600 hover:text-white") outline-none">
        <div class="flex items-center justify-center gap-1"><p class="text-xs md:text-base">Shorten URL </p><span class="hidden md:block">🔗</span></div>
    </button>

    <button @onclick="() => SetActiveComponent(2)" class="w-full px-2 py-[0.499rem] md:px-5 md:py-2 text-center rounded-lg border-[1px] border-orange-600 @(SharedState.ActiveComponent == 2 ? "bg-orange-600 text-white hover:bg-white hover:text-black" : "bg-white hover:bg-orange-600 hover:text-white") transition-all duration-300 shadow-lg outline-none">
        <div class="flex items-center justify-center gap-1">
            <p class="text-xs md:text-base">QR code </p>
            <img src="https://openmoji.org/data/color/svg/E1D8.svg" alt="img" class="hidden md:block md:w-[25px] md:h-25px] object-cover">
        </div>
    </button>

    <button @onclick="() => SetActiveComponent(3)" class="w-full p-2 md:px-5 md:py-3 text-center rounded-lg border-[1px] border-orange-600 @(SharedState.ActiveComponent == 3 ? "bg-orange-600 text-white hover:bg-white hover:text-black" : "bg-white hover:bg-orange-600 hover:text-white") transition-all duration-300 shadow-lg outline-none">
        <div class="flex items-center justify-center gap-1"><p class="text-xs md:text-base">Analytics </p><span class="hidden md:block">📊</span></div>
    </button>

    
</div>


@code {
    private void hey() {
        Console.WriteLine("hey");
    }
    private void SetActiveComponent(int componentIndex) {
        Console.WriteLine("My debug output.");
        SharedState.ActiveComponent = componentIndex;
    }
}